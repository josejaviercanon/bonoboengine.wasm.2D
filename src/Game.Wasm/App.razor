@using Microsoft.JSInterop
@inject IJSRuntime JS

<Routes />

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (!firstRender) return;
        // Interop is initialized via WasmInterop in Program.cs (JSHost.ImportAsync).
    }
}